extends base.pug

block content
    video(src="/"+video.fileUrl, controls, width=800, height=400)
    
    //- div
    //-     p=video.description
    //-     small=video.createdAt
    //- div 
    //-     small Uploaded by 
    //-     a(href=`/user/${video.owner._id}`)=video.owner.name
    
    //- if loggedInUser && (String(video.owner.id) === String(loggedInUser._id))
    //-     a(href=`${video.id}/edit`) Edit Video &rarr;
    //-     br
    //-     a(href=`${video.id}/delete`) Delete Video &rarr;

block scripts 
    script(src="/static/js/videoPlayer.js")