extends base.pug

block content 
    if errorMessage 
        span=errorMessage 

    form(method="POST")
        input(placeholder="User Name",name="username", type="text", required) 
        input(placeholder="Password",name="password", type="password", required) 
        input(type="submit", value="Login")
        br
        a(target="_black", href="/users/github/start") Continue with GitHub &rarr;
    hr
    div 
        span Don'y have an account? 
        a(href="/join") Create now &rarr;
